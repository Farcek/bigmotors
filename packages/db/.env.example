# DBConfig: db:migrate and db:reset. Example only.
# Use a dedicated migration role, not website/admin credentials. Reset requires schema ownership.
DATABASE_URL=postgresql://migration_user:CHANGE_ME@127.0.0.1:5432/bigmotors
