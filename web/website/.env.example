DATABASE_URL=postgres://USER:PASSWORD@127.0.0.1:64401/bigmotors
DATABASE_POOL_MIN=0
DATABASE_POOL_MAX=10
FILES_ROOT=E:/projects/bigmotors/bm-website/infra/.docker/files
# Local disk mode only; writable even when FILES_ROOT is read-only.
# FILES_CACHE=E:/bigmotors-cache/website

# Optional server-only origin. When set, /files uses Sysop instead of local disk.
# Railway: Website needs no files volume; do not append /api or /files.
# FILES_API_BASE_URL=http://sysop-server.railway.internal:4000
